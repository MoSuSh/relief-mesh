services:
    postgres:
        image: postgres:16-alpine
        container_name: reliefmesh
        environment:
            POSTGRES_DB: reliefmesh
            POSTGRES_USER: admin
            POSTGRES_PASSWORD: password123
        ports:
            - "5432:5432"
        volumes:
            - pgdata:/var/lib/postgresql/data

volumes:
 pgdata: